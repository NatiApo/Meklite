# Meklite Backend

Backend Node.js & Express REST API with PostgreSQL for the Meklite web application.

## Setup Instructions

1. Install Node.js (v18 or higher).
2. Install dependencies:
   ```bash
   npm install
